#include <stdlib.h>
#include <stdio.h>
/**
 * main - give a alphabetic in lowercase
 * Return: 0
 */
int main(void)
{
	long int c;

	for (c = '0' ; c <= '99' ; c++)
	{
		if (c <= 99)
		{
			putchar(c);
			putchar(c);
			if (c <= 98)
			{
				putchar(',');
				putchar(' ');
			}
		}
	}
	putchar('\n');
	return (0);
}
