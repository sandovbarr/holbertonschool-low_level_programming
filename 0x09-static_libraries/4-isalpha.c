/**
 * _isalpha - give a alphabetic in lowercas
 * @c: integer
 * Return: integer
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
